namespace("kap")
 	
∇ (keys) (fn ⌸) (values) {
	a ← ∪keys
	a ,[0.5] {⍞fn values /⍨ keys≡¨⊂⍵}¨ a
}

declare(:export ⌸)

⎕p ⇐ (⊣ io:print)
⎕pl ⇐ (⊣ io:println)

⎕A ← @A…@Z
⎕a ← @a…@z
⎕d ← @0…@9

declare(:export (⎕p ⎕pl ⎕A ⎕a ⎕d))
declare(:const (⎕A ⎕a ⎕d))
